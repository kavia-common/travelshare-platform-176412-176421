# TripTales - Environment variables example
# Copy to .env and fill values accordingly.

# MongoDB connection string for Mongoose
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority
