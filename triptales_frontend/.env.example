# Required environment variables for TripTales frontend API to connect to MongoDB
# Copy to .env and fill values appropriately.

# MongoDB connection string, e.g.:
# mongodb+srv://<user>:<password>@<cluster-url>/<db>?retryWrites=true&w=majority
MONGODB_URI=
